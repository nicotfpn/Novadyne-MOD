Before making any code changes to this project, you MUST first consult these two documentation sources for the relevant Minecraft version (26.1.2):

1. **NeoForge Documentation** — https://github.com/neoforged/Documentation.git
   Fetch the relevant `.md` file from `docs/` tree for the API you're working with (e.g. `docs/items/armor.md` for armor, `docs/items/index.md` for items, `docs/concepts/registries.md` for registries).

2. **GeckoLib** — https://github.com/bernie-g/geckolib.git
   Check the GeckoLib wiki or source for the version matching `geckolib-neoforge-26.1.2-5.5.1.jar`.

Do NOT guess API signatures, package paths, or class names. Always verify against these docs first. If the docs don't cover the exact case, look at decompiled vanilla sources in the Gradle cache matching this MC version.
